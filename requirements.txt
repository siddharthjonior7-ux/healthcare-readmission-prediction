# Healthcare Patient Readmission Prediction — Dependencies
# This file grows as new phases are added. Currently covers Phase 3 (EDA).
# Phase 6 will add: scikit-learn, xgboost
# Phase 7 will add: shap

pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
